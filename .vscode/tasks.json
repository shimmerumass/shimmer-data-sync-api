{
    "version": "2.0",
    "tasks": [
        {
            "label": "Run FastAPI Server",
            "type": "shell",
            "command": "uvicorn main:app --reload",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
